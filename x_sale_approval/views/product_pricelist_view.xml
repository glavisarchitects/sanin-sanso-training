<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="x_sale_approval_product_pricelist_form_inherit" model="ir.ui.view">
            <field name="name">x_sale_approval_product_pricelist.form</field>
            <field name="model">product.pricelist</field>
            <field name="inherit_id" ref="product.product_pricelist_view"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='country_group_ids']" position="after">
                    <!-- <field name="x_organization_id"/>-->
                    <field name="org_ids" widget="many2many_tags"/>
                </xpath>
            </field>
        </record>

        <record id="x_sale_approval_view_tree" model="ir.ui.view">
            <field name="name">x_sale_approval_view_tree</field>
            <field name="model">product.pricelist</field>
            <field name="inherit_id" ref="product.product_pricelist_view_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='name']" position="after">
                    <!-- <field name="x_organization_id"/>-->
                    <field name="org_ids" widget="many2many_tags"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>